import React from 'react';
import ContentLoader from '../common/ContentLoader';

const PromotionalImageLoader = () => (
  <>
    <ContentLoader
      css={{
        width: '6rem'
      }}
    />
  </>
);

export default PromotionalImageLoader;
